import styled from 'styled-components'

const WrapperProjketer = styled.div`
    padding: 50px;
`;

const Projekt = styled.div`
`;

const FlexBox = styled.div`
    display: flex;
    padding: 20px 0;
    p {
        border: 1px solid #888;
    }
`;

const TextBox = styled.div`
    padding: 20px 0;
`;

export default WrapperProjketer;
export { Projekt, FlexBox, TextBox }